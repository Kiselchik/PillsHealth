package diplom.dev.aidhealth.db

import diplom.dev.aidhealth.db.MyDbNameClass.TABLE_NAME

class DbGeneralAdapter {
   /*  val DATABASE_NAME = "MyDb"
    val DATABASE_VERSION = 5
    val CREATE_TABLE_DOCTOR = "CREATE TABLE  ${DbHandler.} ("+
            "${DbHandler.COL_ID} INTEGER PRIMARY KEY AUTOINCREMENT, ${COL_DOC_FIRST_NAME} TEXT, "+
            "${COL_DOC_LAST_NAME} TEXT, ${COL_ADDRESS} TEXT,"+
            "${COL_POSITION} TEXT)"*/
}